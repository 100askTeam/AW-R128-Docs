import{_ as e,o as a,c as n,e as s}from"./app-e85d5a28.js";const d={},t=s(`<h1 id="gui-图形系统" tabindex="-1"><a class="header-anchor" href="#gui-图形系统" aria-hidden="true">#</a> GUI 图形系统</h1><p>针对 R128 平台，提供了LVGL作为图形系统。</p><h2 id="lvgl-说明" tabindex="-1"><a class="header-anchor" href="#lvgl-说明" aria-hidden="true">#</a> LVGL 说明</h2><p>LVGL 是一个免费的开源图形库，提供了创建嵌入式GUI 所需的一切，具有易于使用的图形元素，美观的视觉效果和低内存占用，采用MIT 许可协议，可以访问LittlevGL官网获取更多资料。</p><ul><li>强大的构建块：按钮、图表、列表、滑块、图像等。</li><li>高级图形引擎：动画、抗锯齿、不透明度、平滑滚动、混合模式等。</li><li>支持各种输入设备：触摸屏、鼠标、键盘、编码器、按钮等。</li><li>支持多显示器。</li><li>独立于硬件，可与任何微控制器和显示器一起使用。</li><li>可扩展以使用少量内存（64 kB 闪存、16 kB RAM）运行。</li><li>多语言支持，支持UTF-8 处理、CJK、双向和阿拉伯语。</li><li>通过类CSS 样式完全可定制的图形元素。</li><li>受CSS 启发的强大布局：Flexbox 和Grid。</li><li>支持操作系统、外部内存和GPU，但不是必需的。</li><li>使用单个帧缓冲区也能平滑渲染。</li><li>用C 编写并与C++ 兼容。</li><li>Micropython Binding 在Micropython 中公开LVGL API。</li><li>可以在PC 上使用模拟器开发。</li><li>100 多个简单的例子。</li><li>在线和PDF 格式的文档和API 参考。</li></ul><p>目前RTOS 中移植了LVGL 8.1.0 核心组件与Demo，下表列出LVGL 相关库说明：</p><table><thead><tr><th>包名</th><th>说明</th></tr></thead><tbody><tr><td>lv_demos</td><td>lvgl的官方demo</td></tr><tr><td>lv_drivers</td><td>lvgl的官方设备驱动程序，集成了sunxifb、sunxig2d和sunximem</td></tr><tr><td>lv_examples</td><td>lvgl测试用例，最终调用的是lv_demos中的函数</td></tr><tr><td>lvgl</td><td>lvgl核心库</td></tr><tr><td>lv_g2d_test</td><td>g2d测试用例，专门测试已经对接好的g2d接口</td></tr><tr><td>lv_monitor</td><td>压力测试与状态监测软件</td></tr><tr><td>sunxifb.mk</td><td>公共配置文件，写应用Makefile时需要包含进去</td></tr></tbody></table><h2 id="lvgl-配置" tabindex="-1"><a class="header-anchor" href="#lvgl-配置" aria-hidden="true">#</a> LVGL 配置</h2><p><code>mrtos_menuconfig</code> 配置如下</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>System components <span class="token operator">--</span><span class="token operator">-&gt;</span>
    thirdparty components <span class="token operator">--</span><span class="token operator">-&gt;</span>
        <span class="token punctuation">[</span><span class="token operator">*</span><span class="token punctuation">]</span> Littlevgl<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">--</span><span class="token operator">-&gt;</span>
        <span class="token punctuation">[</span><span class="token operator">*</span><span class="token punctuation">]</span> lvgl examples （lvgl官方demo）
        <span class="token punctuation">[</span> <span class="token punctuation">]</span> lvgl g2d test （g2d 接口测试用例）
        <span class="token punctuation">[</span><span class="token operator">*</span><span class="token punctuation">]</span> lvgl<span class="token operator">-</span><span class="token number">8.1</span><span class="token number">.0</span> use sunxifb <span class="token keyword">double</span> buffer （使能双缓冲，解决撕裂问题）
        <span class="token punctuation">[</span><span class="token operator">*</span><span class="token punctuation">]</span> lvgl<span class="token operator">-</span><span class="token number">8.1</span><span class="token number">.0</span> use sunxifb cache （使能fb cache）
        <span class="token punctuation">[</span> <span class="token punctuation">]</span> lvgl<span class="token operator">-</span><span class="token number">8.1</span><span class="token number">.0</span> use sunxifb g2d （使能G2D硬件加速）
        <span class="token punctuation">[</span> <span class="token punctuation">]</span> lvgl<span class="token operator">-</span><span class="token number">8.1</span><span class="token number">.0</span> use sunxifb g2d rotate （使能G2D硬件旋转）
        <span class="token punctuation">[</span> <span class="token punctuation">]</span> lvgl<span class="token operator">-</span><span class="token number">8.1</span><span class="token number">.0</span> use freetype （使能freetype）
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="lvgl-框架" tabindex="-1"><a class="header-anchor" href="#lvgl-框架" aria-hidden="true">#</a> LVGL 框架</h2><p>LVGL 源码位于 <code>lichee/rtos-components/thirdparty/littlevgl-8</code>， 其框架如图：</p><p><img src="http://photos.100ask.net/aw-r128-docs/rtos/developer-guide/part1/chapter6/image1.png" alt=""></p><h3 id="sunxifb" tabindex="-1"><a class="header-anchor" href="#sunxifb" aria-hidden="true">#</a> SunxiFB</h3><p>在 <code>sunxifb</code> 中，我们提供了一组显示接口，如下：</p><table><thead><tr><th>接口</th><th>说明</th></tr></thead><tbody><tr><td><code>sunxifb_init</code></td><td>该函数主要功能是初始化显示引擎。带一个旋转参数，使能g2d旋转的话，就用这个参数指定旋转方向</td></tr><tr><td><code>sunxifb_exit</code></td><td>该函数比较简单，实现关闭<code>cache</code>，关闭<code>g2d</code>，释放旋转<code>buffer</code>，关闭<code>fb0</code></td></tr><tr><td><code>sunxifb_flush</code></td><td>该函数比较重要，负责把<code>draw buffer</code>拷贝到<code>back buffer</code>中，并且绘制最后一帧后，交换<code>front</code>与<code>back buffer</code>。应用不要调用该函数</td></tr><tr><td><code>sunxifb_get_sizes</code></td><td>该函数获取屏幕分辨率，这样应用程序就可以不用写死初始化时的分辨率了</td></tr><tr><td><code>sunxifb_alloc</code></td><td>该函数主要用来申请系统绘图内存，使能部分<code>g2d</code>功能后，会申请连续物理内存</td></tr><tr><td><code>sunxifb_free</code></td><td>该函数用来释放<code>sunxifb_alloc</code>申请的内存</td></tr></tbody></table><p>代码位置如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>lichee<span class="token operator">/</span>rtos<span class="token operator">-</span>components<span class="token operator">/</span>thirdparty<span class="token operator">/</span>littlevgl<span class="token operator">-</span><span class="token number">8</span><span class="token operator">/</span>lv_drivers<span class="token operator">/</span>display<span class="token operator">/</span>sunxifb<span class="token punctuation">.</span>c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在<code>sunxifb_init(rotated)</code>，中<code>rotated</code>的旋转值可以为：</p><p><code>LV_DISP_ROT_NONE</code>，<code>LV_DISP_ROT_90</code>，<code>LV_DISP_ROT_180</code>，<code>LV_DISP_ROT_270</code>。</p><p>最后还有赋值 <code>disp_drv.rotated = rotated</code> 。如果没有g2d旋转，也可以指定<code>disp_drv.sw_rotate = 1</code> 使用软件旋转。</p><h3 id="sunxig2d" tabindex="-1"><a class="header-anchor" href="#sunxig2d" aria-hidden="true">#</a> sunxig2d</h3><p>在 <code>sunxig2d</code> 中，实现了对<code>g2d</code> <code>ioctl</code>的封装，这些函数都<strong>不需要应用调用</strong>，但能加深对整个框架的理解，相关的接口如下：</p><table><thead><tr><th>接口</th><th>说明</th></tr></thead><tbody><tr><td><code>sunxifb_g2d_init</code></td><td><code>g2d</code> 模块初始化函数，打开 <code>/dev/g2d</code> 节点，设置 <code>g_format</code>。初始化时，根据使能的宏，打印相应的 <code>log</code></td></tr><tr><td><code>sunxifb_g2d_deinit</code></td><td>该函数关闭 <code>g2d</code> 设备</td></tr><tr><td><code>sunxifb_g2d_get_limit</code></td><td>该函数获取 <code>g2d</code> 使用阈值</td></tr><tr><td><code>sunxifb_g2d_blit_to_fb</code></td><td>该函数用来拷贝 <code>fb0</code> 的 <code>front</code> 和 <code>back buffer</code> 这两块 <code>buffer</code>，也可以把 <code>rotate buffer</code> 旋转到 <code>back buffer</code></td></tr><tr><td><code>sunxifb_g2d_fill</code></td><td>该函数使用g2d填充一个颜色矩形，颜色可以带透明度</td></tr><tr><td><code>sunxifb_g2d_blit</code></td><td>该函数用来拷贝图像，不能 <code>blend</code> 图像</td></tr><tr><td><code>sunxifb_g2d_blend</code></td><td>该函数可以进行图像 <code>blend</code></td></tr><tr><td><code>sunxifb_g2d_scale</code></td><td>该函数用来缩放图像</td></tr></tbody></table><p>代码位置如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>lichee<span class="token operator">/</span>rtos<span class="token operator">-</span>components<span class="token operator">/</span>thirdparty<span class="token operator">/</span>littlevgl<span class="token operator">-</span><span class="token number">8</span><span class="token operator">/</span>lv_drivers<span class="token operator">/</span>display<span class="token operator">/</span>sunxig2d<span class="token punctuation">.</span>c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>以上 <code>g2d</code> 函数，都已经对接 <code>lvgl</code> 绘图框架，使用 <code>lvgl</code> 的 <code>lv_draw_map</code>、<code>lv_img_set_zoom</code> 和 <code>lv_canvas_draw_img</code> 函数就可以使用起来。</p><p><code>lv_g2d_test</code> 应用中有完整的使用示例。</p><h3 id="sunximem" tabindex="-1"><a class="header-anchor" href="#sunximem" aria-hidden="true">#</a> sunximem</h3><p>在<code>sunximem</code>中，实现了管理物理内存的封装，这些函数都<strong>不需要应用调用</strong>，但能加深对整个框架的理解，如下：</p><table><thead><tr><th>接口</th><th>说明</th></tr></thead><tbody><tr><td><code>sunxifb_mem_init</code></td><td>该函数会在<code>sunxifb_init</code>中调用，初始化物理内存申请接口，使用的是 <code>libuapi</code> 中间件</td></tr><tr><td><code>sunxifb_mem_deinit</code></td><td>该函数通过调用 <code>SunxiMemClose</code>，释放申请的接口资源</td></tr><tr><td><code>sunxifb_mem_alloc</code></td><td>该函数比较重要，许多地方都会用到，需要传入申请的字节数和使用说明</td></tr><tr><td><code>sunxifb_mem_free</code></td><td>该函数用来释放调用 <code>sunxifb_mem_alloc</code> 申请的内存</td></tr><tr><td><code>sunxifb_mem_get_phyaddr</code></td><td>该函数把 <code>sunxifb_mem_alloc</code> 申请内存的虚拟地址转换为物理地址，<code>g2d</code> 驱动只接受 <code>buffer</code> 的物理地址或者fd</td></tr><tr><td><code>sunxifb_mem_flush_cache</code></td><td>该函数用来刷 <code>sunxifb_mem_alloc</code> 申请<code>buffer</code> 的 <code>cache</code></td></tr></tbody></table><p>代码位置如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>lichee<span class="token operator">/</span>rtos<span class="token operator">-</span>components<span class="token operator">/</span>thirdparty<span class="token operator">/</span>littlevgl<span class="token operator">-</span><span class="token number">8</span><span class="token operator">/</span>lv_drivers<span class="token operator">/</span>display<span class="token operator">/</span>sunxigmem<span class="token punctuation">.</span>c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>因为<code>g2d</code>驱动只能使用物理连续内存，因此解码图片时，必须要通过 <code>sunxifb_mem_alloc</code> 来申请内存。</p><blockquote><p>当前只实现了bmp、png和gif图片的内存申请，jpeg图片暂未实现</p></blockquote><p>当使用 <code>lv_canvas_set_buffer</code> 时，传入的 <code>buffer</code> 需要是 <code>sunxifb_alloc</code> 申请的 <code>buffer</code>，<code>sunxifb_alloc</code> 中会判断是否需要申请物理连续内存。</p><blockquote><p>自定义画布lv_canvas暂未对接g2d缩放功能</p></blockquote><h3 id="evdev" tabindex="-1"><a class="header-anchor" href="#evdev" aria-hidden="true">#</a> evdev</h3><p>触摸我们用的是<code>lvgl</code>官方的<code>evdev</code></p><p>代码位置如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_drivers/indev/evdev.c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>lvgl 使用触控功能，需在应用lv_drv_conf.h 文件中配置：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token macro property"><span class="token directive-hash">#</span> <span class="token directive keyword">define</span> <span class="token macro-name">USE_EVDEV</span> <span class="token expression"><span class="token number">1</span></span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>应用<code>lv_drv_conf.h</code> 中的<code>EVDEV_NAME</code> 要与所使用的触摸屏驱动对应，例如 <code>lv_examples</code> 的配置文件：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_examples/src/lv_drv_conf.h
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果disp_drv.rotated 指定了旋转90 或者180 度，lvgl 内部会自行旋转触摸坐标，不用触摸驱动内部去旋转触摸坐标。</p><h3 id="color-depth" tabindex="-1"><a class="header-anchor" href="#color-depth" aria-hidden="true">#</a> color depth</h3><p>在应用 <code>lv_conf.h</code> 文件中有关于色彩深度 <code>LV_COLOR_DEPTH</code> 的配置，例如 <code>lv_examples</code> 的配置文件：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_examples/src/lv_conf.h
/*Color depth: 1 (1 byte per pixel), 8 (RGB332), 16 (RGB565), 32 (ARGB8888)*/
#define LV_COLOR_DEPTH 32
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>使用SPI 屏显示，应用 <code>LV_COLOR_DEPTH</code> 必须和屏幕所支持的像素格式匹配；而其他类型的屏，应用<code>LV_COLOR_DEPTH</code> 选择 16 或32 都可正常显示。</p><p><code>LV_COLOR_DEPTH</code> 选择16 位相较于32 位，内存占用更小，帧率更快，但是色彩总数较少，显示渐变颜色可能会过渡不平滑。</p><h2 id="lvgl-新建应用" tabindex="-1"><a class="header-anchor" href="#lvgl-新建应用" aria-hidden="true">#</a> LVGL 新建应用</h2><p>推荐以 <code>lv_g2d_test</code> 为模板，复制一个新项目：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_g2d_test
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在 <code>Makefile</code> 中，需要包含 <code>sunxifb.mk</code>公共配置，在编译应用时会把宏传递下去。方式如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_g2d_test/Makefile
include components/common/thirdparty/littlevgl-8/sunxifb.mk
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>另外可以注意到有以下配置，这些配置需要按需开启，在部分芯片上是不支持 <code>G2D_BLEND</code> 等操作的，只支持简单的旋转功能：</p><div class="language-makefile line-numbers-mode" data-ext="makefile"><pre class="language-makefile"><code><span class="token keyword">ifeq</span> <span class="token punctuation">(</span><span class="token variable">$</span><span class="token punctuation">(</span>CONFIG_LVGL8_USE_SUNXIFB_G2D<span class="token punctuation">)</span>,y<span class="token punctuation">)</span>
CFLAGS<span class="token operator">+=</span>-DLV_USE_SUNXIFB_G2D_FILL \\
        -DLV_USE_SUNXIFB_G2D_BLEND \\
        -DLV_USE_SUNXIFB_G2D_BLIT \\
        -DLV_USE_SUNXIFB_G2D_SCALE
<span class="token keyword">endif</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在应用编译的实际 <code>Makefile</code> 中，可以只编译需要的文件，缩减可执行文件的大小。</p><p>关于lvgl 的配置文件，也是建议用 <code>lv_g2d_test</code> 中的，可以对比原始未修改过的配置，然后再根据实际场景开关相应配置。配置文件如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>lichee<span class="token operator">/</span>rtos<span class="token operator">-</span>components<span class="token operator">/</span>thirdparty<span class="token operator">/</span>littlevgl<span class="token operator">-</span><span class="token number">8</span><span class="token operator">/</span>lv_g2d_test<span class="token operator">/</span>src<span class="token operator">/</span>lv_conf<span class="token punctuation">.</span>h
lichee<span class="token operator">/</span>rtos<span class="token operator">-</span>components<span class="token operator">/</span>thirdparty<span class="token operator">/</span>littlevgl<span class="token operator">-</span><span class="token number">8</span><span class="token operator">/</span>lv_g2d_test<span class="token operator">/</span>src<span class="token operator">/</span>lv_drv_conf<span class="token punctuation">.</span>h
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>原文件：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lvgl/lv_conf_template.h
lichee/rtos-components/thirdparty/littlevgl-8/lv_drivers/lv_drv_conf_template.h.h
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>最后就是应用的初始化了，在 <code>lv_g2d_test</code> 中，有比较清晰的调用流程了，需要注意的是 <code>sunxifb_init</code> 需要传入旋转参数和 <code>sunxifb_alloc</code> 申请内存即可。</p><h2 id="lvgl-运行" tabindex="-1"><a class="header-anchor" href="#lvgl-运行" aria-hidden="true">#</a> LVGL 运行</h2><p>我们提供了几个测试用例，执行命令如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code>lv_examples <span class="token number">0</span><span class="token punctuation">,</span> is lv_demo_widgets
lv_examples <span class="token number">1</span><span class="token punctuation">,</span> is lv_demo_music
lv_examples <span class="token number">2</span><span class="token punctuation">,</span> is lv_demo_benchmark
lv_examples <span class="token number">3</span><span class="token punctuation">,</span> is lv_demo_keypad_encoder
lv_examples <span class="token number">4</span><span class="token punctuation">,</span> is lv_demo_stress
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在初始化时，会有如下打印，根据配置的不同会有差异，表示打开了某项配置：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>wh=800x480, vwh=800x960, bpp=32, rotated=0
Turn on double buffering.
Turn on 2d hardware acceleration.
Turn on 2d hardware acceleration fill.
Turn on 2d hardware acceleration blit.
Turn on 2d hardware acceleration blend.
Turn on 2d hardware acceleration scale.
Turn on 2d hardware acceleration rotate.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="lvgl-开启触控功能" tabindex="-1"><a class="header-anchor" href="#lvgl-开启触控功能" aria-hidden="true">#</a> LVGL 开启触控功能</h2><p><code>lvgl</code> 使用触控功能，需在应用 <code>lv_drv_conf.h</code> 文件中使能宏 <code>USE_EVDEV</code>。前提要确认好触摸屏驱动模块能够正常加载使用，并且使 <code>lv_drv_conf.h</code> 中的 <code>EVDEV_NAME</code> 与所使用的触摸屏驱动对应：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token macro property"><span class="token directive-hash">#</span> <span class="token directive keyword">define</span> <span class="token macro-name">USE_EVDEV</span> <span class="token expression"><span class="token number">1</span></span></span>
<span class="token macro property"><span class="token directive-hash">#</span> <span class="token directive keyword">define</span> <span class="token macro-name">EVDEV_NAME</span> <span class="token string">&quot;touchscreen&quot;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>lvgl 对接触摸屏驱动的代码位置如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>lichee/rtos-components/thirdparty/littlevgl-8/lv_drivers/indev/evdev.c
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="lvgl-开启-log-日志调试" tabindex="-1"><a class="header-anchor" href="#lvgl-开启-log-日志调试" aria-hidden="true">#</a> LVGL 开启 <code>LOG</code> 日志调试</h2><p>lvgl 自带的Log 日志调试功能，在应用 <code>lv_conf.h</code> 文件使能宏 <code>LV_USE_LOG</code> 及相关配置：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>#define LV_USE_LOG 1
#define LV_LOG_LEVEL LV_LOG_LEVEL_INFO
#define LV_LOG_PRINTF 1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>另外在 <code>sunxifb.c</code> 文件或 <code>Makefile</code> 中定义宏 <code>LV_USE_SUNXIFB_DEBUG</code>，可以 Log 打印出LVGL 实际刷图显示的帧率。</p><h2 id="spi-屏显示-lvgl-画面异常" tabindex="-1"><a class="header-anchor" href="#spi-屏显示-lvgl-画面异常" aria-hidden="true">#</a> SPI 屏显示 LVGL 画面异常</h2><p>应用 <code>lv_conf.h</code> 文件中的<code>LV_COLOR_DEPTH</code> 要和SPI 屏所支持的像素格式匹配：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>/*Color depth: 1 (1 byte per pixel), 8 (RGB332), 16 (RGB565), 32 (ARGB8888)*/
#define LV_COLOR_DEPTH 32
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,81),o=[t];function c(l,i){return a(),n("div",null,o)}const p=e(d,[["render",c],["__file","chapter6.html.vue"]]);export{p as default};
